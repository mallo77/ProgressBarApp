<div class="progress large-12 small-12 columns">
	<span id="bar{{id}}" class="meter {{#if default}}active{{/if}}" data-value="{{value}}" style="width: {{value}}%">
		<p class="percentage">{{value}}%</p>
	</span>
</div>